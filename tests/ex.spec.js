describe('test group', () => {
    beforeAll(() => {

    })
    afterAll(() => {

    })
    beforeEach(() => {

    })
    afterEach(() => {

    })

    test('test case', () => {
        const actual = ['one', 'two', 'three']
        expect(actual).toEqual(['one', 'two', 'three'])
    })
})